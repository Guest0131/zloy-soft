[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="В схеме пользоывателя user создайте политику тщательного контроля доступа price_policy для таблицы wine, с функцией безопасности price_policy_func."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="RLS"
TopicId=42

; Автор задачи
Author="BUL"

; Имя решателя (не изменять)
Solver="PCRE"

;BEGIN DBMS_RLS.add_policy(
;	policy_name => 'price_policy',
;	object_schema => user,
;	object_name => 'WINE',
;	function_schema => user,
;	policy_function =>'price_policy_func'
;	);
;end;



[DataBase]
TaskId=874
GroupTaskId=874
