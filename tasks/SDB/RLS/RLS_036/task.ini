[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Добавте политику EMP_POLICY для таблицы EMP, принадлежащей схеме HR. Эта политика должна применять фильтр, задаваемой функцией AUTHORIZED_EMPS, на операцию INSERT."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="RLS"
TopicId=42

; Автор задачи
Author="Rom"

; Имя решателя (не изменять)
Solver="PCRE"

;begin DBMS_RLS.add_policy( object_shema=>'User', object_name=>'EMP', policy_name=>'EMP_POLICY', ;function_schema=>'HR', policy_function=>'AUTHORIZED_EMPS', statement_types=>'INSERT' );end;



[DataBase]
TaskId=897
GroupTaskId=897
