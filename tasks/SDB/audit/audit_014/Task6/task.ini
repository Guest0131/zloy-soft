[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Выведите все обращения к объектам с именем CLIENTS из журнала аудита (вывести имя пользователя(username), владельца(owner), имя объекта(obj_name), SES_ACTIONS)"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="Аудит"
TopicId=43

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

;
;
;
;SELECT USERNAME, OWNER, ACTION_NAME, SES_ACTIONS
;FROM   USER_AUDIT_TRAIL
;where obj_name = 'CLIENTS';



[DataBase]
TaskId=672
GroupTaskId=672
