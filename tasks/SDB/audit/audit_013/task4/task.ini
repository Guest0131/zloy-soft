[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Выведите все обращения к объектам с именем CLIENT из журнала аудита (вывести имя пользователя(username), владельца(owner), имя объекта(obj_name), SES_ACTIONS)"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="Аудит"
TopicId=43

; Автор задачи
Author="DAN"

; Имя решателя (не изменять)
Solver="PCRE"

;select username, owner, obj_name, ses_actions from user_audit_trail where obj_name='client'



[DataBase]
TaskId=701
GroupTaskId=701
