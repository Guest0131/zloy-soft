[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Вы только что включили аудит записав в нужный параметр нужное значение. Убедитесь, что аудит включен. При обращении к таблице вывести столбцы: name, value."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="Аудит"
TopicId=43

; Автор задачи
Author="Ast"

; Имя решателя (не изменять)
Solver="PCRE"

;SELECT name, value FROM v$parameter WHERE name LIKE 'audit_trail%';



[DataBase]
TaskId=658
GroupTaskId=658
