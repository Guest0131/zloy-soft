[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Выведите все попытки доступа к базе данных из журнала аудита вывести строки с полями: имя пользователя(username), терминал(terminal), время(timestamp) и результат того успешный ли вход был(returncode)"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="Аудит"
TopicId=43

; Автор задачи
Author="BUL"

; Имя решателя (не изменять)
Solver="PCRE"

;select username, terminal, timestamp, returncode from user_audit_session



[DataBase]
TaskId=677
GroupTaskId=677
