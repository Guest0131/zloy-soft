[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создайте таблицу КЛИЕНТ (client) с полями: id_client - id клиента( типа NUMBER), 
surname - фамилия клиента (типа VARCHAR2(100)),
passport - номер паспорта клиента (типа VARCHAR2(100)),
telephone_number - номер телефона клиента (типа VARCHAR2(100)).</p>
<p>Поле passport должно быть зашифрованным, без указания каких-либо дополнительных параметров."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="TDE"
TopicId=46

; Автор задачи
Author="Rezlazy"

; Имя решателя (не изменять)
Solver="PCRE"

;CREATE TABLE client(id_client NUMBER, surname VARCHAR2(100), passport VARCHAR2(100) encrypt, telephone_number VARCHAR2(100))



[DataBase]
TaskId=835
GroupTaskId=835
