[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Добавьте шифрующийся столбец price типа INTEGER в таблицу dishes, установив алгоритм AES256.
В этой же команде добавьте столбец name типа VARCHAR2(50), который шифруется без соли"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="TDE"
TopicId=46

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

; ALTER TABLE dishes ADD (name VARCHAR2(50) ENCRYPT NO SALT, price INTEGER ENCRYPT USING 'AES256');



[DataBase]
TaskId=817
GroupTaskId=817
