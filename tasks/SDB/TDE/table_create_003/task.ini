[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создайте таблицу cars у которой поле failures зашифровано алгоритмом AES256. 
Поля в таблице: id INTEGER PRIMARY KEY, numb VARCHAR2(20), failures NUMBER
"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="TDE"
TopicId=46

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

;CREATE TABLE cars(
;id INTEGER PRIMARY KEY,
;numb VARCHAR2(20),
;failures NUMBER ENCRYPT USING 'AES256'
;);



[DataBase]
TaskId=830
GroupTaskId=830
