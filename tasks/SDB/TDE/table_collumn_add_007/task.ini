[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Добавьте шифрующийся столбец manager типа VARCHAR2(50) в таблицу employees, установив алгоритм AES128.
В этой же команде добавьте столбец name типа VARCHAR2(50), который шифруется без соли"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="TDE"
TopicId=46

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

; ALTER TABLE employees ADD (name VARCHAR2(50) ENCRYPT NO SALT, manager VARCHAR2(50) ENCRYPT USING 'AES128');



[DataBase]
TaskId=818
GroupTaskId=818
