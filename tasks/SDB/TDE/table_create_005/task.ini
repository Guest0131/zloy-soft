[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создайте таблицу delivery_service у которой поле office_address зашифровано алгоритмом 3DES168, поле phone_number - без соли.
Поля в таблице: id INTEGER PRIMARY KEY, name VARCHAR2(50), phone_number VARCHAR2(50), office_address VARCHAR2(50)
"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="TDE"
TopicId=46

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

;CREATE TABLE delivery_service(
;id INTEGER PRIMARY KEY,
;name VARCHAR2(50),
;phone_number VARCHAR2(50) ENCRYPT NO SALT,
;office_address VARCHAR2(50) ENCRYPT USING '3DES168'
;);



[DataBase]
TaskId=832
GroupTaskId=832
