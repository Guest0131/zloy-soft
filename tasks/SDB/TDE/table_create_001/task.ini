[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создайте таблицу clients у которой поле orders_amount зашифровано алгоритмом по умолчанию. 
Поля в таблице: id INTEGER PRIMARY KEY, name VARCHAR2(50), orders_amount NUMBER(10,3)
"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="TDE"
TopicId=46

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

;CREATE TABLE clients(
;id INTEGER PRIMARY KEY,
;name VARCHAR2(50),
;orders_amount NUMBER(10,3) ENCRYPT
;);



[DataBase]
TaskId=828
GroupTaskId=828
