[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создайте таблицу employees у которой поле manager зашифровано алгоритмом AES128, поле name - без соли.
Поля в таблице: id INTEGER PRIMARY KEY, name VARCHAR2(50), manager VARCHAR2(50)
"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="TDE"
TopicId=46

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

;CREATE TABLE employees(
;id INTEGER PRIMARY KEY,
;name VARCHAR2(50) ENCRYPT NO SALT,
;manager VARCHAR2(50) ENCRYPT USING 'AES128'
;);



[DataBase]
TaskId=834
GroupTaskId=834
