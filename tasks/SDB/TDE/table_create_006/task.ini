[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создайте таблицу dishes у которой поле price зашифровано алгоритмом AES256, поле name - без соли.
Поля в таблице: id INTEGER PRIMARY KEY, name VARCHAR2(50), price INTEGER
"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="TDE"
TopicId=46

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

;CREATE TABLE dishes(
;id INTEGER PRIMARY KEY,
;name VARCHAR2(50) ENCRYPT NO SALT,
;price INTEGER ENCRYPT USING 'AES256'
;);



[DataBase]
TaskId=833
GroupTaskId=833
