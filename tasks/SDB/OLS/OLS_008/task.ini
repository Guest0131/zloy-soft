[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создать политику мандатного управления доступом DOCS_POLICY для разгарничения доступа на чтение, так чтобы метки хранились в столбце OLS_SECURITY."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Ast"

; Имя решателя (не изменять)
Solver="PCRE"

;begin
; SA_SYSDBA.CREATE_POLICY (
;   policy_name      => 'DOCS_POLICY',
;   column_name      => 'OLS_SECURITY', 
;   default_options  => 'read_control');
;end;



[DataBase]
TaskId=911
GroupTaskId=911
