[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создать политику POLICY для разгарничения доступа на чтение, так чтобы метки хранились в столбце ols_service"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="DAN"

; Имя решателя (не изменять)
Solver="PCRE"

;begin SA_SYSDBA.CREATE_POLICY(
;policy_name => POLICY,
;column_name => ols_service,
;default_options => 'read_control')
;end



[DataBase]
TaskId=939
GroupTaskId=939
