[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Для политики мандатного управления доступом emp_pol создать метку с числовым идентификатором 90010 и текстовым представлением метки H::RO."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Look"

; Имя решателя (не изменять)
Solver="PCRE"

;BEGIN
;  SA_LABEL_ADMIN.CREATE_LABEL (
;   policy_name     => 'emp_pol',
;   label_tag       => 90010,
;   label_value     => 'H::RO',
;   data_label      => TRUE);
;END;



[DataBase]
TaskId=938
GroupTaskId=938
