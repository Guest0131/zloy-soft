[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text=" К таблице CLIENTS из схемы REST применить политику clients_pol для разгарничения доступа на чтение. В схеме уже реализована функция генерации меток label_function(varchar2 region, number orders_amount)."

; Сложность (1-5)
Difficulty=2

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

;BEGIN
;SA_POLICY_ADMIN.APPLY_TABLE_POLICY (
;	policy_name=>'clients_pol',
;	schema_name=>'REST', 
;	table_name=>'CLIENTS',
;	table_options=>'READ_CONTROL', 
;	label_function=>'clients_label(:new.region,:new.Orders_amount)', 
;	predicate      =>NULL);
;END;



[DataBase]
TaskId=950
GroupTaskId=950
