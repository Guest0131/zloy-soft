[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="С схеме SCOTT создана политика DOCS_POLICY для разграничения доступа на чтение и реализована функция генерации меток documents_label(access_control VARCHAR2, type VARCHAR2). Необходимо применить эту политику к таблице DOCUMENTS.; Сложность (1-5)"

Difficulty=2

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Ast"

; Имя решателя (не изменять)
Solver="PCRE"

;BEGIN
;  SA_POLICY_ADMIN.APPLY_TABLE_POLICY (
;   policy_name    => 'DOCS_POLICY',
;   schema_name    => 'SCOTT', 
;   table_name     => 'DOCUMENTS',
;   table_options  => 'READ_CONTROL', 
;   label_function => 'documents_label(:new.access_control,:new.type)', 
;   predicate      => NULL);  
;END;



[DataBase]
TaskId=915
GroupTaskId=915
