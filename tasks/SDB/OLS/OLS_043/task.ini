[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создать политику clients_pol для разграничения доступа на чтение. При создании политики, столбец для хранения меток не указывать."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Mus"

; Имя решателя (не изменять)
Solver="PCRE"

;BEGIN
;SA_SYSDBA.CREATE_POLICY (
;policy_name=>'clients_pol',
;default_options=>'read_control'); 
;END;



[DataBase]
TaskId=946
GroupTaskId=946
