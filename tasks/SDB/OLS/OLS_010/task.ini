[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="В базе данных создана политика DOCS_POLICY. В компании, эксплуатирующей данную базу данных существует двухуровневое разграничение по отделам, с корневым отделом, имеющим сокращенный идентификатор MN. Необходимо создать дочерний для MN отдел с полным именем FIRST_SALON, сокращенным FS, и числовым идентификатором 200"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Ast"

; Имя решателя (не изменять)
Solver="PCRE"

;BEGIN
;SA_COMPONENTS.CREATE_GROUP (
;   policy_name     => 'DOCS_POLICY',
;   group_num       => 200,
;   short_name      => 'FS',
;   long_name       => 'FIRST_SALON',
;   parent_name     => 'MN'); 
;END;



[DataBase]
TaskId=913
GroupTaskId=913
