[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="В базе данных создана политика emp_pol. В этой политике существует трехуровневое подчинение с главной группой RO. Необходимо создать дочернюю для группы RO группу с полным именем MASTER, сокращенным MA, и числовым идентификатором 20"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Look"

; Имя решателя (не изменять)
Solver="PCRE"

;BEGIN
;SA_COMPONENTS.CREATE_GROUP (
;   policy_name     => 'emp_pol',
;   group_num       => 20,
;   short_name      => 'MA',
;   long_name       => 'MASTER',
;   parent_name     => 'RO');
;END;



[DataBase]
TaskId=934
GroupTaskId=934
