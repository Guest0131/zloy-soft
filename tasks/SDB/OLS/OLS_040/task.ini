[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="С схеме DAN создана политика POLICY для разграничения доступа на чтение и реализована функция генерации меток service_label(hall VARCHAR2, price NUMBER). Необходимо применить эту политику к таблице SERVICE_OLS."

; Сложность (1-5)
Difficulty=2

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="DAN"

; Имя решателя (не изменять)
Solver="PCRE"

;begin SA_POLICY_ADMIN.APPLY_TABLE_POLICY(
;policy_name => 'POLICY'",
;schema_name => 'DAN'",
;table_name => 'SERVICE_OLS',
;table_options => 'READ_CONTROL,
;label_function => 'service_label (:new.hall, :new.price)',
;predicate => NULL);
;end;



[DataBase]
TaskId=943
GroupTaskId=943
