[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="В схеме creator создана политика rate_policy для разграничения доступа на удаление и реализована функция генерации меток rate_label (bit_money INTEGER, coefficient FLOAT). Необходимо применить эту политику к таблице rate."

; Сложность (1-5)
Difficulty=2

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Sidor"

; Имя решателя (не изменять)
Solver="PCRE"

;begin
; SA_POLICY_ADMIN.APPLY_TABLE_POLICY (
;  policy_name    => 'rate_policy',
;  schema_name    => 'CREATOR', 
;  table_name     => 'RATE',
;  table_options  => 'delete_control', 
;  label_function => 'rate_label(:new.bit_money,:new.coefficient)',
;  predicate      => NULL);
;  end;



[DataBase]
TaskId=964
GroupTaskId=964
