[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="В схеме BDRom создана политика my_policy для разграничения доступа на запись и реализована функция генерации меток get_label(product VARCHAR2, amount NUMBER). Необходимо применить эту политику к таблице Category_Product."

; Сложность (1-5)
Difficulty=2

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Rom"

; Имя решателя (не изменять)
Solver="PCRE"

;begin SA_POLICY_ADMIN.APPLY_TABLE_POLICY(
;           policy_name => 'my_policy',
;			schema_name => 'BDRom',
;			table_name  => 'Category_Product',
;			table_options => 'WRITE_CONTROL',
;			label_function => 'get_label(:new.product,:new.amount)',
;			predicate => NULL
;);
;end



[DataBase]
TaskId=957
GroupTaskId=957
