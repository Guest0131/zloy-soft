[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создать политику emp_pol для разграничения доступа на чтение, так чтобы метки хранились в столбце ols_employees"

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Look"

; Имя решателя (не изменять)
Solver="PCRE"

;begin
;SA_SYSDBA.CREATE_POLICY (
;  policy_name      => 'emp_pol', 
;  column_name      => 'ols_employees', 
;  default_options  => 'read_control'); 
;end;



[DataBase]
TaskId=932
GroupTaskId=932
