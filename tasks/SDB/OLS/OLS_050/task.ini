[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="Создать политику c именем my_policy для разгарничения доступа на запись, так чтобы метки хранились в столбце labels."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Rom"

; Имя решателя (не изменять)
Solver="PCRE"

;
;begin SA_SYSDBA.CREATE_POLICY (
;	policy_name => 'my_policy',
;   column_name => 'labels',
;   default_options => 'write_control'
;);
;end



[DataBase]
TaskId=953
GroupTaskId=953
