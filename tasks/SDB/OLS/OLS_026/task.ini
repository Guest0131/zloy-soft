[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="В схеме CREATOR  действует политика мандатного управления доуступом clients_pol, которая применена к таблице clients. Необходимо  ее отключить."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="OLS"
TopicId=52

; Автор задачи
Author="Kam"

; Имя решателя (не изменять)
Solver="PCRE"

; BEGIN
; sa_policy_admin.disable_table_policy (
      ; policy_name      => 'clients_pol', -- название политики
      ; schema_name      => 'creator', -- название схемы
      ; table_name       => 'clients'); -- название таблицы, к которой применялась политика
; END;



[DataBase]
TaskId=929
GroupTaskId=929
