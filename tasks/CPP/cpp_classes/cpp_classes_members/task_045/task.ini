[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Укажите правильные способы вызова метода Method:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
class Klunk
{
public int Method();
};

void main()
{
Klunk klunk;
Klunk* klunkptr = &klunk;
}
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Члены класса"

;ID Темы
TopicId=565

;Автор
Author="Щербаков С. Е."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "klunk.Method()"
ResponseOptions[]="klunk.Method()"
Multi[] = "klunkptr->Method()"
ResponseOptions[]="klunkptr->Method()"
ResponseOptions[]="Klunk::Method()"
ResponseOptions[]="klunk->Method()"
ResponseOptions[]="klunkptr.Method()"
ResponseOptions[]="klunkptr::Method()"
ResponseOptions[]="klunk::Method()"



[DataBase]
TaskId=1808
GroupTaskId=1280
