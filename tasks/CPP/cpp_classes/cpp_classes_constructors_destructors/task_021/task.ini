[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Когда используется конструктор копирования?</p><p></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Конструкторы и деструкторы"

;ID Темы
TopicId=564

;Автор
Author="Щербаков С. Е."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "При инициализации нового объекта уже существующим"
ResponseOptions[]="При инициализации нового объекта уже существующим"
Multi[] = "При передаче объекта по значению"
ResponseOptions[]="При передаче объекта по значению"
Multi[] = "Когда функция возвращает объект"
ResponseOptions[]="Когда функция возвращает объект"
ResponseOptions[]="При динамическом распределении памяти"
ResponseOptions[]="Перед вызовом деструктора"
ResponseOptions[]="Вместо конструктора по умолчанию, если он не определен"



[DataBase]
TaskId=1756
GroupTaskId=1277
