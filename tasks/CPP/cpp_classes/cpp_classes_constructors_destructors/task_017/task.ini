[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=1

;Условие
Text="Что выведет на экран следующий фрагмент кода:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
#include &lt;iostream&gt;

class Klunk
{
public:
Klunk() { number = 0; }
Klunk(int num = 100) { number = num; }
void print() { std::cout &lt;&lt; this-&gt;number; }
private:
int number;
};

void  main()
{
  Klunk klunk;
klunk.print();
}
 </code></pre></p><p></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Конструкторы и деструкторы"

;ID Темы
TopicId=564

;Автор
Author="Щербаков С. Е."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "Ничего, код не скомпилируется"
ResponseOptions[]="Ничего, код не скомпилируется"
ResponseOptions[]="0"
ResponseOptions[]="100"
ResponseOptions[]="Неопределенное значение"



[DataBase]
TaskId=1752
GroupTaskId=1277
