[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=1

;Условие
Text="Что выведет на экран следующий фрагмент кода?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
#include &lt;iostream&gt;

class Klunk
{
public:
Klunk(int num = 0) { number = num; }
Klunk(const Klunk&) { number = 10; }
void print() { std::cout &lt;&lt; this-&gt;number; }
private:
int number;
};

void  main()
{
  Klunk klunk1(20);
Klunk klunk2(30);
klunk2 = klunk1;
klunk1.print();
}
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Конструкторы и деструкторы"

;ID Темы
TopicId=564

;Автор
Author="Щербаков С. Е."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "20"
ResponseOptions[]="20"
ResponseOptions[]="10"
ResponseOptions[]="30"
ResponseOptions[]="Ничего"
ResponseOptions[]="0"



[DataBase]
TaskId=1757
GroupTaskId=1277
