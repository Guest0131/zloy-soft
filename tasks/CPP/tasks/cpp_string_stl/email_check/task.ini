[Info]

Variant=0

Text="<p>Написать функцию std::string email_check(std::string email), которая проверяет введенный email на корректность 
<p>- Email не должен содержать символы кириллицы(Возвращает строку \"Cyrillic symbols!\")</p>
<p>- Email не должен содеражать символы !, ? , {}, [], (), &, ^, %, $, *(Возвращает строку \"Invalid symbols!\")</p>
<p>- Длина имени почтового ящика не должна превышать 64 символа(Возвращает строку \"Email name is too long!\")</p>
<p>Функция должна возвращать строку "Email is correct", если email корректный.</p>"

Difficulty=2

Topic="Задачи на String и STL"
TopicId=611
Author="Akberov"
Solver="CPP"



[DataBase]
TaskId=1286
GroupTaskId=1052
