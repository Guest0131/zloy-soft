[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=1

;Условие
Text="Чем отличаются записи  в угловых скобках(&lt;...&gt;) и двойных кавычках (“…”) при подключении заголовочного файла?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Модели памяти, пространства имен"

;ID Темы
TopicId=561

;Автор
Author="Пимков П.А."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "Если имя файла помещено в угловые скобки, то компилятор ищет его в той части базовой файловой системы, где расположены стандартные заголовочные файлы."
ResponseOptions[]="Если имя файла помещено в угловые скобки, то компилятор ищет его в той части базовой файловой системы, где расположены стандартные заголовочные файлы."
Multi[] = " Если имя файла помещено в двойные кавычки, то компилятор ищет файл в текущем рабочем каталоге или в каталоге с исходным кодом, а затем в стандартном местоположении"
ResponseOptions[]=" Если имя файла помещено в двойные кавычки, то компилятор ищет файл в текущем рабочем каталоге или в каталоге с исходным кодом, а затем в стандартном местоположении"
ResponseOptions[]="Если имя файла помещено в двойные кавычки, то компилятор ищет его в той части базовой файловой системы, где расположены стандартные заголовочные файлы."
ResponseOptions[]="Если имя файла помещено в угловые скобки, то компилятор ищет файл в текущем рабочем каталоге или в каталоге с исходным кодом, а затем в стандартном местоположении"



[DataBase]
TaskId=1450
GroupTaskId=1195
