[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=4

;Условие
Text="Что произойдет при выполнение следующего фрагмента кода?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
namespace Jill { 
double fetch; 
}

char fetch; 

int main () 
{ 
using Jill; 
double fetch; 
cin &gt;&gt; fetch; 
…
}
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Модели памяти, пространства имен"

;ID Темы
TopicId=561

;Автор
Author="Карбушев В.И."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Ничего не произойдет – код с ошибкой"
ResponseOptions[]="Ничего не произойдет – код с ошибкой"
ResponseOptions[]="Чтение значения в локальную переменную fetch"
ResponseOptions[]="Чтение значения в глобальную переменную fetch"
ResponseOptions[]="Чтение значения в переменную Jill::fetch"



[DataBase]
TaskId=1440
GroupTaskId=1195
