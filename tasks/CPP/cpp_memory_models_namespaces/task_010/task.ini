[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Применение ключевого слова static к функции:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Модели памяти, пространства имен"

;ID Темы
TopicId=561

;Автор
Author="Желонкин А.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Ограничивает область видимости этой функции"
ResponseOptions[]="Ограничивает область видимости этой функции"
ResponseOptions[]="Указывает компилятору встраивать код функции в место вызова"
ResponseOptions[]="Указывает компилятору, что начиная со второго вызова функция всегда возвращает то, что было возвращено во время её первого вызова"



[DataBase]
TaskId=1427
GroupTaskId=1185
