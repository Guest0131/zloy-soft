[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Укажите в каких строках функция regex_search() использует корректное пространство имен:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Функции для работы с регулярными выражениями"

;ID Темы
TopicId=602

;Автор
Author="Боловцов С.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "std::tr1::regex_search(str.c_str(), res, rx);"
ResponseOptions[]="std::tr1::regex_search(str.c_str(), res, rx);"
ResponseOptions[]="regex_search(str.c_str(), res, rx);"
ResponseOptions[]="std::regex_search(str.c_str(), res, rx);"
ResponseOptions[]="tr1::regex_search(str.c_str(), res, rx);"
ResponseOptions[]="boost::regex_search(str.c_str(), res, rx);"
ResponseOptions[]="boost::tr1::regex_search(str.c_str(), res, rx);"



[DataBase]
TaskId=1491
GroupTaskId=1195
