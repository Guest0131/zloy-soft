[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Расположите по порядку символы в регулярном выражении, чтобы функция regex_match вернула true</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
// вместо 123456789 необходимо подставить в правильном порядке символы
// регулярного выражения
// Подсказка: \w лучше использовать раньше \s 
std::string str = “Its string object with words”;
std::cmatch result;
std::regex regular(“123456789”);
if (std::regex_match(str.c_str(), result, regular))
   std::cout &lt;&lt; “true” &lt;&lt; endl;
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Функции для работы с регулярными выражениями"

;ID Темы
TopicId=602

;Автор
Author="Боловцов С.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "order"



[DataBase]
TaskId=1495
GroupTaskId=1195
