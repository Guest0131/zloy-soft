[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=2

;Условие
Text="Что вернет функция regex_match в следующем примере:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
std::string text = “Hello world”;
std::regex reg(“Hello (\\w)*”);
regex_match(text.begin(), text.end(), reg);
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Функции для работы с регулярными выражениями"

;ID Темы
TopicId=602

;Автор
Author="Шлейников Ф.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "true"
ResponseOptions[]="true"
ResponseOptions[]="false"



[DataBase]
TaskId=1490
GroupTaskId=1195
