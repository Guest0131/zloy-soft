[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Какое значение будет в переменной a2?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
0  bool a1 = false, a2 = false;
1  std::string str = “Hello world”;
2  std::tr1::regex rx(“ello”);
3  a1 = regex_match(str.begin(), str.end(), rx);
4  a2 = regex_search(str.begin(), str.end(), rx);
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Функции для работы с регулярными выражениями"

;ID Темы
TopicId=602

;Автор
Author="Боловцов С.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "text"

Text[] = "true"



[DataBase]
TaskId=1493
GroupTaskId=1195
