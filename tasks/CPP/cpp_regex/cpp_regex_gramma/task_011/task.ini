[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Каков результат выполнения данного фрагмента программы?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
std::string a = “D0gsAndC4ts”;
std::regex b(“Dogs(\\w)*”);
if (regex_match(a.begin(), a.end(), b))
std::cout &lt;&lt; “Strings matched!”;
else
std::cout &lt;&lt; “Strings didn’t match!”;
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=4

;Тема
Topic="Грамматика регулярных выражений"

;ID Темы
TopicId=601

;Автор
Author="Шлейников Ф.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Strings didn’t match!"
ResponseOptions[]="Strings didn’t match!"
ResponseOptions[]="Strings matched!"



[DataBase]
TaskId=1525
GroupTaskId=1200
