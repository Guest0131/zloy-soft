[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Какие функция способна считывает символы пробела и новой строки, не прерывая ввод на них?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="I. Состояния потока"

;ID Темы
TopicId=594

;Автор
Author="PvVn"

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "get(char &)"
ResponseOptions[]="get(char &)"
Multi[] = "get(void)"
ResponseOptions[]="get(void)"
Multi[] = "read()"
ResponseOptions[]="read()"
Multi[] = "operator>>()"
ResponseOptions[]="operator>>()"
Multi[] = "getline()"
ResponseOptions[]="getline()"



[DataBase]
TaskId=2052
GroupTaskId=1322
