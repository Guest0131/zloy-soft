[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Выберете методы не применимые для cout:*---</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="I. Состояния потока"

;ID Темы
TopicId=594

;Автор
Author="Марюков В.О."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "cout.is_number()"
ResponseOptions[]="cout.is_number()"
Multi[] = "cout.size()"
ResponseOptions[]="cout.size()"
ResponseOptions[]="cout.fill()"
ResponseOptions[]="cout.flush()"
ResponseOptions[]="cout.setf()"
ResponseOptions[]="cout.precision()"



[DataBase]
TaskId=2063
GroupTaskId=1333
