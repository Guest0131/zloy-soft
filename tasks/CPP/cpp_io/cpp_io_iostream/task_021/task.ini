[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Включение в программу файла iostream автоматически создает потоковые объекты. Потоку ошибок соответствуют объекты:</p><p>Выберите утверждения относящиеся к функции tmpnam():</p><p>Некоторая программа содержит следующий фрагмент. При использовании перенаправления вывода в файл на экране могут оказаться только строки:</p><p><pre class=\"prettyprint linenums no-scroll\"><code> 
cout &lt;&lt; “Here come the goodies!\n”;
cerr &lt;&lt; “Something horrible has happened.\n”;
exit(1);
</code></pre></p><p></p><p>Выберите верные утверждения:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Семейство классов iostream."

;ID Темы
TopicId=592

;Автор
Author="Марюков В.О."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "Объект cin представляет стандартный ввод в виде потока байтов"
ResponseOptions[]="Объект cin представляет стандартный ввод в виде потока байтов"
Multi[] = "Извлечение символов из потока подразумевает преобразование типа"
ResponseOptions[]="Извлечение символов из потока подразумевает преобразование типа"
Multi[] = "Пробельные символы при извлечении из потока с помощью cin пропускаются"
ResponseOptions[]="Пробельные символы при извлечении из потока с помощью cin пропускаются"
ResponseOptions[]="Объекты cin извлекают из потока только символы типа char"
ResponseOptions[]="cin не применима для работы с указателями"



[DataBase]
TaskId=2041
GroupTaskId=1319
