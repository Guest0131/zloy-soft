[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Завершите предложение. Создание объекта ostream...</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Общие понятия"

;ID Темы
TopicId=591

;Автор
Author="pv"

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "открывает поток;"
ResponseOptions[]="открывает поток;"
Multi[] = "автоматически создаёт буфер;"
ResponseOptions[]="автоматически создаёт буфер;"
Multi[] = "ассоциирует буфер с потоком;"
ResponseOptions[]="ассоциирует буфер с потоком;"
ResponseOptions[]="закрывает поток;"
ResponseOptions[]="ждёт пока будет создан буфер;"
ResponseOptions[]="ждёт ассоциации буфера с потоком;"
ResponseOptions[]="ждёт открытия потока;"
ResponseOptions[]="создаёт буфер по запросу;"
ResponseOptions[]="снимает ассоциацию буфера с потоком;"



[DataBase]
TaskId=2015
GroupTaskId=1310
