[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Если файл с таким именем существует, что произойдёт с ним после команды open()?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Файловый ввод-вывод"

;ID Темы
TopicId=595

;Автор
Author="PvVn"

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Станет доступен для записи, но он будет усечён так, чтобы запись осуществлялась в пустой файл"
ResponseOptions[]="Станет доступен для записи, но он будет усечён так, чтобы запись осуществлялась в пустой файл"
ResponseOptions[]="Откроется, что с ним ещё произойдёт?"
ResponseOptions[]="Станет доступен, но не сразу, а спустя 10 секунд"
ResponseOptions[]="Станет доступен для записи, но все имеющиеся в нём данные будут перезаписаны в бинарном виде"



[DataBase]
TaskId=2065
GroupTaskId=1335
