[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Расположите строки программы так, чтобы в результате выполнения функции TerminateTesting() были получены следующие строки:</p><p>«Внутри блока try</p><p>Внутри нового обработчика: вызов функции exit()»</p><p>Примечание:</p><p>Для подмены стандартной функции terminate() определяется собственная функция типа terminate_function с прототипом</p><p>void my_terminate();</p><p>и указано её имя, вызвав функцию set_terminate():</p><p>set_terminate(my_terminate);// Установка собственной функции завершения выполнения программы</p><p>После этого вместо terminate() при обработке неперехваченных исключений будет вызываться собственная функция.</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=4

;Тема
Topic="Проталкивание исключений"

;ID Темы
TopicId=582

;Автор
Author="Боловцов С.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "order"



[DataBase]
TaskId=1101
GroupTaskId=1011
