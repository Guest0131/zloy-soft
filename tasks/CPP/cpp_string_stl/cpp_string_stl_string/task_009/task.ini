[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=2

;Условие
Text="Что делает следующий код?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
cin.getline(charr, 20);
 </code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Строки"

;ID Темы
TopicId=585

;Автор
Author="Корсун Н.Д."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Читает строку в массив"
ResponseOptions[]="Читает строку в массив"
ResponseOptions[]="Читает строку в структуру данных"
ResponseOptions[]="Читает строку в объект string"



[DataBase]
TaskId=1111
GroupTaskId=1017
