[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Выберите метод(ы) класса string для определения размера строки:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Строки"

;ID Темы
TopicId=585

;Автор
Author="Цецерский А.И."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "length()"
ResponseOptions[]="length()"
Multi[] = "size()"
ResponseOptions[]="size()"
ResponseOptions[]="len()"
ResponseOptions[]="strsize()"
ResponseOptions[]="strlen()"
ResponseOptions[]="npos()"
ResponseOptions[]="pos()"



[DataBase]
TaskId=1120
GroupTaskId=1017
