[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Выберите правильное описание к перегурженному методу find</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
size_type find (const string & str, size_type pos = 0) const;
</code></pre></p><p></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Строки"

;ID Темы
TopicId=585

;Автор
Author="Корсун И.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Ищет в вызывающей строке первое вхождение подстроки str, начиная с позиции pos. Возвращает индекс первого символа найденной подстроки или string::npos, если подстрока не найдена"
ResponseOptions[]="Ищет в вызывающей строке первое вхождение подстроки str, начиная с позиции pos. Возвращает индекс первого символа найденной подстроки или string::npos, если подстрока не найдена"
ResponseOptions[]="Метод задан не верно"
ResponseOptions[]="Ищет в вызывающей строке первое вхождение подстроки, начиная с позиции pos. Возвращает все индексы символов найденной подстроки или string::npos, если подстрока не найдена"



[DataBase]
TaskId=1130
GroupTaskId=1017
