[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Чем функция data() отличается от функции c_str():</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Строки"

;ID Темы
TopicId=585

;Автор
Author="Шлейников Ф.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "data()не добавляет нуль-символ в конец строки"
ResponseOptions[]="data()не добавляет нуль-символ в конец строки"
ResponseOptions[]="data()возвращает константную строку "
ResponseOptions[]="data()приводит строку к типу const char*"
ResponseOptions[]="Обе функции идентичны"



[DataBase]
TaskId=1114
GroupTaskId=1017
