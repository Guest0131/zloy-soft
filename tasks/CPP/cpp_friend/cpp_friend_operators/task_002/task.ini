[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Дано объявление класса, восстановите порядок реализации оператора вывода:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
class Screen {
   friend istream&
      operator&gt;&gt;( istream&, Screen& );
   friend ostream&
      operator&lt;&lt;( ostream&, const Screen& );
public:
   // ... оставшаяся часть класса Screen
};
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Дружественные операторы"

;ID Темы
TopicId=576

;Автор
Author="Боловцов С.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "order"



[DataBase]
TaskId=1986
GroupTaskId=1296
