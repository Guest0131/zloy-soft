[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Оператор цикла while выполняется при  0≤x≤5, x=40 и при нечетных x, больших 50. Выберите правильное выражение-условие:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Циклы"

;ID Темы
TopicId=553

;Автор
Author="Демидова А.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "0<=x && x <= 5 || x == 40 || x > 50 && x%2!=0"
ResponseOptions[]="0<=x && x <= 5 || x == 40 || x > 50 && x%2!=0"
ResponseOptions[]="0<=x<=5 || x==40 || x>50 && x%2!=0"
ResponseOptions[]="(0<=x && x<=5) && (x==40) && (x%2!=0 && x>50)"
ResponseOptions[]="x>=0 && x>=5 || x=40 || x>50 && x%2!=0"
ResponseOptions[]="x>=0 && x>=5 || x==40 || x%2<>0 && x>50"



[DataBase]
TaskId=1831
GroupTaskId=1280
