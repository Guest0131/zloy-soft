[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=1

;Условие
Text="Укажите пример правильного синтаксиса цикла for:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Циклы"

;ID Темы
TopicId=553

;Автор
Author="Каримов Р.Д."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "for (инициализация;  условие;  итератор) { операторы(ы) ; }"
ResponseOptions[]="for (инициализация;  условие;  итератор) { операторы(ы) ; }"
ResponseOptions[]="for (итератор;  условие;  инициализация) { операторы(ы) ; }"
ResponseOptions[]="for (инициализация;  итератор;  ;  условие) { операторы(ы) ; }"



[DataBase]
TaskId=1892
GroupTaskId=1284
