[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Выберите все корректные логические выражения, которые можно разместить в круглых скобках для проверки условия x  [a,b]:</p><p>if (...)...</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Операторы ветвления"

;ID Темы
TopicId=552

;Автор
Author="Петухова Н.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "x < a || x > b"
ResponseOptions[]="x < a || x > b"
Multi[] = "!(x >= a && x <= b)"
ResponseOptions[]="!(x >= a && x <= b)"
Multi[] = "!(x >= a) || !(x <= b)"
ResponseOptions[]="!(x >= a) || !(x <= b)"
ResponseOptions[]="x < a && x > b"
ResponseOptions[]="!(x >= a || x <= b)"
ResponseOptions[]="!(x >= a) && !(x <= b)"



[DataBase]
TaskId=1909
GroupTaskId=1284
