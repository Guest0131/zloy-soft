[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Какой из вариантов эквивалентен выражению “if (17 &lt;age &lt;35)”?</p><p></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Операторы ветвления"

;ID Темы
TopicId=552

;Автор
Author="Кравченко О.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "if ( (17 < age ) < 35)"
ResponseOptions[]="if ( (17 < age ) < 35)"
ResponseOptions[]="if (age > 17  && age < 35)"
ResponseOptions[]="If (17 < ( age < 35) )"
ResponseOptions[]="if (age > 17  || age < 35)"



[DataBase]
TaskId=1949
GroupTaskId=1284
