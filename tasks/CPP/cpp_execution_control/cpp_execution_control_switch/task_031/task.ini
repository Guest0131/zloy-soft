[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Какие утверждения относительно оператора switch верны?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Операторы ветвления"

;ID Темы
TopicId=552

;Автор
Author="Каримов Р.Д."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "Если целочисленное значение переменной count не совпадает ни с одним константным выражением, то управление передается ветви программы, содержащей зарезервированное слово default"
ResponseOptions[]="Если целочисленное значение переменной count не совпадает ни с одним константным выражением, то управление передается ветви программы, содержащей зарезервированное слово default"
Multi[] = "Оператор switch может содержать, а может не содержать зарезервированное слово default"
ResponseOptions[]="Оператор switch может содержать, а может не содержать зарезервированное слово default"
Multi[] = "Если значение переменной не совпадет ни с одним константным выражением и не будет default, то программное управление перешло бы к первому оператору после switch"
ResponseOptions[]="Если значение переменной не совпадет ни с одним константным выражением и не будет default, то программное управление перешло бы к первому оператору после switch"
ResponseOptions[]="Если значение переменной не совпадет ни с одним константным выражением и не будет default, то программное управление в этом случае перейдет к первому case"
ResponseOptions[]="Меткой switch может быть только переменная"



[DataBase]
TaskId=1939
GroupTaskId=1284
