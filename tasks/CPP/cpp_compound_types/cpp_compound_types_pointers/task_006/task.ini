[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Что будет выведено на экране после выполнения фрагмента программы: </p><p>int x, *ptr, **dptr;</p><p>x = 45;</p><p>ptr = &x;</p><p>dptr = &ptr;</p><p>cout &lt;&lt; **dptr &lt;&lt; endl;</p><p>Указатель нужен для того, чтобы...</p><p></p><p>Что произойдет при добавлении единицы к переменной типа “указатель”</p><p></p><p>Выберете правильные утверждения</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
void main() 
{
double * pn;
double bubble = 3.2;
pn = bubble;
pa = new double[30]
}
</code></pre></p><p>Как правильно выделять динамическую память</p><p>Для чего нужна операция delete?</p><p>Выберете правильные утверждения</p><p>Что появится на экран после выполнения данного кода?</p><p>#include &lt;iostream&gt;</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
int main()
{
int * intArray = new int[3];
std::cout &lt;&lt; &intArray[0];
return 0;
}
</code></pre></p><p></p><p>Что появится на экране после выполнения данного кода?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
#include &lt;iostream&gt;
int main()
{
int * mas = new int[3] {5, 3, 8};
std::cout &lt;&lt; mas[0];
return 0;
}
</code></pre></p><p>Выберите правильные утверждения об автоматическом хранилище?</p><p>Что такое статическое хранилище?</p><p>Что произойдет, если не вызвать delete после создания переменной с помощью операции new в свободном хранилище?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Указатели"

;ID Темы
TopicId=549

;Автор
Author="Липовцев И. C. Зыков И.С."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Переменная или конструкция, динамически выделенная в области свободного хранилища, останется там, даже при условии, что память, содержащая указатель, будет освобождена в соответствии с правилами видимости и временем жизни объекта"
ResponseOptions[]="Переменная или конструкция, динамически выделенная в области свободного хранилища, останется там, даже при условии, что память, содержащая указатель, будет освобождена в соответствии с правилами видимости и временем жизни объекта"
ResponseOptions[]="Переменная или конструкция, динамически выделенная в области свободного хранилища, останется там, если только память, содержащая указатель, не будет освобождена в соответствии с правилами видимости и временем жизни объекта"
ResponseOptions[]="После работы с этой памятью она все равно самостоятельно удалится"



[DataBase]
TaskId=1033
GroupTaskId=998
