[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Если объявленным возвращаемым типом функции указан double, а функция возвращает выражение int, то</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Возврат из функций"

;ID Темы
TopicId=557

;Автор
Author="pv"

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "то int приводится к double"
ResponseOptions[]="то int приводится к double"
ResponseOptions[]="возвращается тип double"
ResponseOptions[]="возвращается тип float (double обрезается до размеров float, т.к. sizeof(int)=sizeof(float))"
ResponseOptions[]="программа не компилируется"



[DataBase]
TaskId=1208
GroupTaskId=1030
