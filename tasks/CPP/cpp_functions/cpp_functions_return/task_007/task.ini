[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="После выполнения оператора return:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Возврат из функций"

;ID Темы
TopicId=557

;Автор
Author="pv"

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "функция завершается"
ResponseOptions[]="функция завершается"
ResponseOptions[]="выполнение переходит к следующему оператору"
ResponseOptions[]="в стек помещается возвращаемое значение, а выполнение переходит к следующему оператору"
ResponseOptions[]="функция завершается, если это последний return"



[DataBase]
TaskId=1210
GroupTaskId=1030
