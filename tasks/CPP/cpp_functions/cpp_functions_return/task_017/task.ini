[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Пусть think() – некая функция. Укажите верные утверждения:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Возврат из функций"

;ID Темы
TopicId=557

;Автор
Author="Пимков П.А."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "process(think) – передача адреса think() функции process() "
ResponseOptions[]="process(think) – передача адреса think() функции process() "
Multi[] = "thought(think()) – передача возвращаемого значения think() функции thought()"
ResponseOptions[]="thought(think()) – передача возвращаемого значения think() функции thought()"
ResponseOptions[]="process(think) – передача возвращаемого значения think() функции process()"
ResponseOptions[]="thought(think()) – передача адреса think() функции thought()"



[DataBase]
TaskId=1220
GroupTaskId=1038
