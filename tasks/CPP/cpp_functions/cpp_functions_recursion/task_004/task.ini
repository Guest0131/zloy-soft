[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=3

;Условие
Text="Выберите запись строки (???) для правильной реализации рекурсивной функции вычисления факториала числа n:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
int fact(int n)
{
(???)
return n * fact(n - 1);
}</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Рекурсия"

;ID Темы
TopicId=558

;Автор
Author="Петухова Н.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "if (n == 0) return 1;"
ResponseOptions[]="if (n == 0) return 1;"
ResponseOptions[]="if (n == 1) return 0;"
ResponseOptions[]="if (n == 1) return 1;"
ResponseOptions[]="if (n == 2) return 2;"



[DataBase]
TaskId=1224
GroupTaskId=1040
