[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=1

;Условие
Text="Выберите определение функции, использующее рекурсию, если функция вычисления суммы целых чисел от 1 до n имеет прототип int sum(int n):</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Рекурсия"

;ID Темы
TopicId=558

;Автор
Author="Петухова Н.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "{if (n == 1) return 1;   return n + sum(n - 1);}"
ResponseOptions[]="{if (n == 1) return 1;   return n + sum(n - 1);}"
ResponseOptions[]="{if (n == 1) return 1;   return sum(n);}"
ResponseOptions[]="{if (n == 1) return 1;   return sum(n) + (n - 1);}"
ResponseOptions[]="{if (n == 1) return 1;   return sum(n) + sum(n - 1);}"



[DataBase]
TaskId=1222
GroupTaskId=1040
