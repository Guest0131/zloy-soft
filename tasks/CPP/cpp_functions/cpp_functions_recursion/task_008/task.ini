[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Дана рекурсивная функция:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
int function(int n)
{
  if (n == 0) 
    return 1;
  return n* function (n - 1);
}
</code></pre></p><p>Верно ли написана эта же функция, построенная уже без использования рекурсий:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
int function (int n)
{
  if(n == 0)
    return 1;
  int temp = 1;
  for(int i = 1; i &lt;= n; i++)
  {
    temp = temp*i;
  }
  return temp;
}
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Рекурсия"

;ID Темы
TopicId=558

;Автор
Author="Кравченко О. В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Да"
ResponseOptions[]="Да"
ResponseOptions[]="Нет"



[DataBase]
TaskId=1228
GroupTaskId=1040
