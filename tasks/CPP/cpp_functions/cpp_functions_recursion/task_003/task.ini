[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=2

;Условие
Text="Функция вычисления суммы целых чисел от start до end имеет прототип int sum(int start, int end); Как записать определение функции, используя рекурсию?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Рекурсия"

;ID Темы
TopicId=558

;Автор
Author="Петухова Н.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "{if (end == start) return start;   return end + sum(start, end -1);}"
ResponseOptions[]="{if (end == start) return start;   return end + sum(start, end -1);}"
ResponseOptions[]="{if (end == start) return 1;   return end + sum(start, end -1);}"
ResponseOptions[]="{if (end >= start) return 0;  return end + sum(start, end -1);}"
ResponseOptions[]="{if (end != start) return start;  return end + sum(start, end -1);}"



[DataBase]
TaskId=1223
GroupTaskId=1040
