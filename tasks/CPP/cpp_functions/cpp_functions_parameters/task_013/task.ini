[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=4

;Условие
Text="Когда функция вызывается, компьютер выделяет память, необходимую для ЭТИХ переменных. Когда функция завершается, компьютер освобождает память, которая была использована ЭТИМИ переменными. ЭТИ переменные называются:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Аргументы функций"

;ID Темы
TopicId=556

;Автор
Author="Пимков П.А."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Локальными переменными"
ResponseOptions[]="Локальными переменными"
ResponseOptions[]="Формальными аргументами (формальными параметрами)"
ResponseOptions[]="Фактическими аргументами (фактическими параметрами)"
ResponseOptions[]="Автоматическими переменными"



[DataBase]
TaskId=1246
GroupTaskId=1042
