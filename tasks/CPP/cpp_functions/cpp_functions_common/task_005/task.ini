[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Отметьте все истинные высказывания:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Общие понятия"

;ID Темы
TopicId=555

;Автор
Author="Петухова Н.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "если функция не принимает никаких параметров, то скобки или оставляются пустыми, или в них записывается ключевое слово void"
ResponseOptions[]="если функция не принимает никаких параметров, то скобки или оставляются пустыми, или в них записывается ключевое слово void"
ResponseOptions[]="переменные, объявленные внутри функции, существуют только во время выполнения тела функции"
ResponseOptions[]="после завершения выполнения функции она уничтожается и память, занимаемая этой функцией, освобождается"
ResponseOptions[]="глобальные переменные «затеняют» (скрывают) в теле функции локальные переменные с тем же именем"



[DataBase]
TaskId=1259
GroupTaskId=1042
