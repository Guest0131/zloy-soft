[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Запись ::func(x) означает, что</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Общие понятия"

;ID Темы
TopicId=555

;Автор
Author="Петухова Н.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "вызывается функция из глобального контекста"
ResponseOptions[]="вызывается функция из глобального контекста"
ResponseOptions[]="функция func написана на ассемблере"
ResponseOptions[]="вызывается функция из анонимного контекста"
ResponseOptions[]="вызывается функция, определенная в другом файле"
ResponseOptions[]="функция func имеет тип void"



[DataBase]
TaskId=1261
GroupTaskId=1044
