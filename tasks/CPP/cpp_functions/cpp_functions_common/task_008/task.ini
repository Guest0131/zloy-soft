[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Укажите правильные объявления функции:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Общие понятия"

;ID Темы
TopicId=555

;Автор
Author="Петухова Н.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "double Average2(int arr[], int size);"
ResponseOptions[]="double Average2(int arr[], int size);"
Multi[] = "float Seg(double w, char k, int x = 0);"
ResponseOptions[]="float Seg(double w, char k, int x = 0);"
Multi[] = "bool IsSorted(double *arr, int size);"
ResponseOptions[]="bool IsSorted(double *arr, int size);"
ResponseOptions[]="int MyFunc(double x, y, int i);"
ResponseOptions[]="double Average1(double *arr; int size);"
ResponseOptions[]="void F3(double d = 3.14, double a);"
ResponseOptions[]="char * FF(char *str1; char *str2; int len);"



[DataBase]
TaskId=1262
GroupTaskId=1045
