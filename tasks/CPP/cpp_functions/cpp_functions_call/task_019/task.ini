[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=6

;Условие
Text="Выберите верные утверждения:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
int sloth = 3;
const int * ps = &sloth;
int * const finger = &sloth; 
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=4

;Тема
Topic="Использование функций"

;ID Темы
TopicId=559

;Автор
Author="Пимков П.А."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "ps – указатель на const int"
ResponseOptions[]="ps – указатель на const int"
Multi[] = "finger – const-указатель на int"
ResponseOptions[]="finger – const-указатель на int"
ResponseOptions[]="ps – const-указатель на int"
ResponseOptions[]="finger – указатель на const int"



[DataBase]
TaskId=1200
GroupTaskId=1030
