[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Какое значение будет содержаться в переменной result после возврата из вызова функции f2(10, 2, result)?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>void f2(int x, int n, int y)
{
int i;
for(i = 0, y = 1; i &lt; n; ++i)
y = y * x;
}</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Использование функций"

;ID Темы
TopicId=559

;Автор
Author="Петухова Н.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "результат не определен"
ResponseOptions[]="результат не определен"
ResponseOptions[]="10"
ResponseOptions[]="100"
ResponseOptions[]="1000"



[DataBase]
TaskId=1184
GroupTaskId=1029
