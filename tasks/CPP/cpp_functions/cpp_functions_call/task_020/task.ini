[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Указан прототип функции pam(). Выберите верное объявление соответствующего типа указателя(на функцию):</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
double pam(int);
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=4

;Тема
Topic="Использование функций"

;ID Темы
TopicId=559

;Автор
Author="Пимков П.А."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "double (*pf) (int); "
ResponseOptions[]="double (*pf) (int); "
ResponseOptions[]="double* pf = pam(int);"
ResponseOptions[]="Double (*pf) = pam(int);"
ResponseOptions[]="double* pf (int);"



[DataBase]
TaskId=1201
GroupTaskId=1030
