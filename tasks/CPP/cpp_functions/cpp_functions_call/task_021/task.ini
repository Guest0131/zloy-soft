[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Выберите верные утверждения:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
double ned(double);
int ted(int);
double(*pf)(int);
pf = ned;
pf = ted;
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=4

;Тема
Topic="Использование функций"

;ID Темы
TopicId=559

;Автор
Author="Пимков П.А."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "pf = ned; операция выполнится некорректно"
ResponseOptions[]="pf = ned; операция выполнится некорректно"
Multi[] = "pf = ted; операция выполнится некорректно"
ResponseOptions[]="pf = ted; операция выполнится некорректно"
ResponseOptions[]="pf = ned; операция выполнится корректно"
ResponseOptions[]="pf = ted; операция выполнится корректно"



[DataBase]
TaskId=1202
GroupTaskId=1030
