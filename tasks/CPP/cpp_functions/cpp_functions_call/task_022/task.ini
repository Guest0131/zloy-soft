[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Выберите верные утверждения:</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
1.double pam(int);
2.double(*pf)(int);
3.pf = pam;
4.double x = pam(4);
5.double y = (*pf)(5);
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=4

;Тема
Topic="Использование функций"

;ID Темы
TopicId=559

;Автор
Author="Пимков П.А."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "1 строка – объявление прототипа функции"
ResponseOptions[]="1 строка – объявление прототипа функции"
Multi[] = "2 строка – объявление указателя на функцию"
ResponseOptions[]="2 строка – объявление указателя на функцию"
Multi[] = "4 строка – вызывается функция pam() по имени"
ResponseOptions[]="4 строка – вызывается функция pam() по имени"
Multi[] = "4 строка – вызывается функция pam() по имени"
ResponseOptions[]="4 строка – вызывается функция pam() по имени"
Multi[] = "5 строка – вызывается функция pam(), используя указатель"
ResponseOptions[]="5 строка – вызывается функция pam(), используя указатель"
ResponseOptions[]="2 строка – объявление указателя на тип double"
ResponseOptions[]="3 строка – pf присваивается возвращаемое значение функции pam()"



[DataBase]
TaskId=1203
GroupTaskId=1030
