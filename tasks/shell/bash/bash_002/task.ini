[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text="В текущем каталоге существует директория folder. Все файлы с одинаковыми расширениями, находящиеся внутри этой папки, поместить в отдельные подкаталоги, для файлов без расширений так же создать отдельный подкаталог."

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="Shell"
TopicId=54

; Автор задачи
Author="Leh"

; Имя решателя (не изменять)
Solver="Shell" ; Обязательно

[Solution]
; Процедура проверки:
; Выполняется запрос пользователя, выполняется эталонный запрос. Ожидается, что результаты совпадут

; Массив запрещенных слов (специфичные для данной задачи)
; ForbiddenWords[]=word1
ForbiddenWords[]=word2

; Массив обязательных слов (специфичные для данной задачи)
;RequiredWords[]=['for','mkdir']



[DataBase]
TaskId=11
GroupTaskId=11
