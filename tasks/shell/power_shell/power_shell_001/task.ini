[Info]

; Вариант задачи - номер по журналу того человека, чей отчет (и, соответственно, предметная область используется)
Variant=0

; Текст задачи
Text=" Создать директорию в текущем каталоге с именем "username" и в ней создать файл file.txt с содержимым "FILE". Скопировать этот файл в директорию с именем пользователя.  
Скопировать файл в корневой каталог.
"

;  Solution
; new-item -path username -itemtype "directory" 
; new-item -path username/file.txt -itemtype file -value "FILE"
; copy-item -path username/file.txt -destination file_copy.txt

; Сложность (1-5)
Difficulty=1

; Тема задачи
Topic="PowerShell"
TopicId=55

; Автор задачи
Author="Serg"

; Имя решателя (не изменять)
Solver="PowerShell" ; Обязательно

[Solution]
; Процедура проверки:
; Выполняется запрос пользователя, выполняется эталонный запрос. Ожидается, что результаты совпадут

; Массив запрещенных слов (специфичные для данной задачи)
; ForbiddenWords[]=word1
ForbiddenWords[]=word2

; Массив обязательных слов (специфичные для данной задачи)
;RequiredWords[]="cp"



[DataBase]
TaskId=21
GroupTaskId=21
