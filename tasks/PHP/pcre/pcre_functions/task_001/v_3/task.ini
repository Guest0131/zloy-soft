[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=3

;Условие
Text="Выберите функцию, производящую замену совпадений:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Функции для работы с регулярными выражениями"
TopicId=199


;Автор
Author="Мезенцев А. С."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "preg_replace()"
ResponseOptions[]="preg_replace()"
ResponseOptions[]="preg_split()"
ResponseOptions[]="preg_match_all()"
ResponseOptions[]="preg_match()"

