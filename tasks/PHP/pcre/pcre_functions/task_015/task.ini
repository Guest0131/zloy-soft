[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Выберите возможные константы для параметра $flag функции preq_match_all</p><p>Формат параметров: (string  $pattern, string  $subject, array  &$matches [,  int  $flags] [,  int  $offset])</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Функции для работы с регулярными выражениями"
TopicId=199


;Автор
Author="Мезенцев А. С."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "PREG_SET_ORDER"
ResponseOptions[]="PREG_SET_ORDER"
Multi[] = "PREG_PATTERN_ORDER"
ResponseOptions[]="PREG_PATTERN_ORDER"
Multi[] = "PREG_OFFSET_CAPTURE"
ResponseOptions[]="PREG_OFFSET_CAPTURE"
ResponseOptions[]="PREG_SPLIT_NO_EMPTY"
ResponseOptions[]="PREG_ORDER"
ResponseOptions[]="PREG_ CAPTURE"

