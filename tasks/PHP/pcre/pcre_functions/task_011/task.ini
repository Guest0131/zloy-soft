[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Что делает следующая функция?</p><p>array  preg_grep(string  $expr,  array  $input  [,  int  $flags  =  0])</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Функции для работы с регулярными выражениями"
TopicId=199


;Автор
Author="Мезенцев А. С."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Возвращает только те строки из массива \$input, для которых было обнаружено совпадение с регулярным выражением \$expr"
ResponseOptions[]="Возвращает только те строки из массива \$input, для которых было обнаружено совпадение с регулярным выражением \$expr"
ResponseOptions[]="Возвращает только те строки из массива \$input, для которых не было обнаружено совпадений с регулярным выражением \$expr"
ResponseOptions[]="Разбивает каждую строку из \$input на несколько в соответствии с регулярным выражением и возвращает их в массиве"
ResponseOptions[]="Среди вариантов ответа нет верного"



[DataBase]
TaskId=2681
GroupTaskId=1742
