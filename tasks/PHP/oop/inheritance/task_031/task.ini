[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Что означает extends в приведенном ниже фрагменте кода?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
class FileLogger
{
    public $a;
    public function __construct($a)
    {
        $this-&gt;$a = $a;
    }
}

classFileLoggerDebug extends FileLogger
{
    private $base;
    public $b;
    public function __construct($a, $b)
    {
        $this-&gt;base = new Base($a);
        $this-&gt;$b = $b;
    }
}
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Наследование"
TopicId=223


;Автор
Author="Шамсутдинов М. Р."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Создаваемый класс FileLoggerDebug является «расширением» класса FileLogger"
ResponseOptions[]="Создаваемый класс FileLoggerDebug является «расширением» класса FileLogger"
ResponseOptions[]="Создаваемый класс FileLoggerявляется «расширением» класса FileLoggerDebug"
ResponseOptions[]="Класс FileLoggerDebug  является копией FileLogger"
ResponseOptions[]="Класс FileLoggerявляется копией FileLoggerDebug"
ResponseOptions[]="Среди вариантов нет правильных"

