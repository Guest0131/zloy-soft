[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Какая функция создает временный файл?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Работа с файлами"
TopicId=249


;Автор
Author="Купцов А.А."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "int tmpfile()"
ResponseOptions[]="int tmpfile()"
ResponseOptions[]="Int tmp()"
ResponseOptions[]="file mktmpfile()"
ResponseOptions[]="int fopen(\"tmp\", \"w+b\");"

