[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=2

;Условие
Text="В листинге представлена ...</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
&lt;?php
$message = \"testing ...\";
$myecho = function(...$Arr) use ($message)
{  
    echo $message; 
    foreach ($Arr[0] as $item) {
        echo \"$item&lt;br /&gt;\n\";
    }
};
$message = \"working ...\";
$myecho(['apple', 'banana', 'cherry']);
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Функции"

;ID Темы
TopicId=196

;Автор
Author="Сорокин С.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "Передача параметра функции по значению"
ResponseOptions[]="Передача параметра функции по значению"
Multi[] = "Анонимная функция"
ResponseOptions[]="Анонимная функция"
Multi[] = "Замыкание"
ResponseOptions[]="Замыкание"
ResponseOptions[]="Условно определяемая функция"
ResponseOptions[]="Передача функции по ссылке"
ResponseOptions[]="Передача параметра функции по ссылке"
ResponseOptions[]="Возврат функцией ссылки"
ResponseOptions[]="Возврат функцией значения"

