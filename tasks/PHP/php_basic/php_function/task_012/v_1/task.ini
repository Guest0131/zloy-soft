[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=1

;Условие
Text="Каков результат выполнения следующего кода?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
&lt;?php
declare(strict_types = 1);

function sum(int $__fst, int $__snd)
{
    return $__fst + $__snd;
}

echo sum(2.5, 2); 
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Функции"

;ID Темы
TopicId=196

;Автор
Author="Сорокин С.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "В зависимости от версии PHP будет сгенерировано исключение или сообщение об ошибке"
ResponseOptions[]="В зависимости от версии PHP будет сгенерировано исключение или сообщение об ошибке"
ResponseOptions[]="4"
ResponseOptions[]="4.5"
ResponseOptions[]="2"
ResponseOptions[]="В зависимости от версии PHP будет выведено сообщение об ошибке или 4.5"

