[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=1

;Условие
Text="Какие функции являются функциями замены в строке?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Замена"
TopicId=242


;Автор
Author="Малыхин В.Е."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "str_replace()"
ResponseOptions[]="str_replace()"
Multi[] = "str_ireplace()"
ResponseOptions[]="str_ireplace()"
Multi[] = "substr_replace()"
ResponseOptions[]="substr_replace()"
ResponseOptions[]="replace()"
ResponseOptions[]="replace_substr()"

