[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=3

;Условие
Text="Какие функции не являются функциями замены в строке?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Замена"
TopicId=242


;Автор
Author="Малыхин В.Е."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "replace_substring()"
ResponseOptions[]="replace_substring()"
Multi[] = "substring_replace()"
ResponseOptions[]="substring_replace()"
Multi[] = "string_replace()"
ResponseOptions[]="string_replace()"
Multi[] = "replace()"
ResponseOptions[]="replace()"
Multi[] = "replace_substr()"
ResponseOptions[]="replace_substr()"

