[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=1

;Условие
Text="С помощью каких функций можно преобразовать timestamp в строковый формат?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Формат даты и функции для работы с ним"
TopicId=205


;Автор
Author="Федотов М.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "date(\$format, \$timestamp)"
ResponseOptions[]="date(\$format, \$timestamp)"
Multi[] = "strftime(\$format, \$timestamp)"
ResponseOptions[]="strftime(\$format, \$timestamp)"
ResponseOptions[]="getdate(\$timestamp)"
ResponseOptions[]="strtotime(\$format, \$timestamp)"
ResponseOptions[]="time(\$format, \$timestamp)"

