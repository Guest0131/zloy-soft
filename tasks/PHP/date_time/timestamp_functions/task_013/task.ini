[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Функция checkdate() может использоваться с параметрами:</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Формат даты и функции для работы с ним"
TopicId=205


;Автор
Author="Федотов М.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "checkdate(\$month, \$day, \$year);"
ResponseOptions[]="checkdate(\$month, \$day, \$year);"
ResponseOptions[]="checkdate(\$date_string, \$date_format);"
ResponseOptions[]="checkdate();"
ResponseOptions[]="checkdate(date());"

