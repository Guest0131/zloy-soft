[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=3

;Условие
Text="Что выведется на экран после выполнения скрипта?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
&lt;?php 
$date = new DateTime(\"2016-01-01 00:00:00\",
new DateTimeZone(\"Europe/Moscow\"));
echo $date-&gt;format(\"d-m-Y H:i:s\");
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Календарные классы php"
TopicId=206


;Автор
Author="Федотов М.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Переданная в качестве параметра дата и время с установленным часовым поясом переданным в качестве параметра"
ResponseOptions[]="Переданная в качестве параметра дата и время с установленным часовым поясом переданным в качестве параметра"
ResponseOptions[]="Текущая дата и время в соответствии с форматом, переданным в качестве параметра"
ResponseOptions[]="Переданная в качестве параметра дата и время, смещённая относительно GMT с установленным часовым поясом переданным в качестве параметра"

