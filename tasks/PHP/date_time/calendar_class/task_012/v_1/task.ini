[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=1

;Условие
Text="Что выведется на экран после выполнения скрипта?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
&lt;?php
$date     = new DateTime('2015-01-01 0:0:0');
Snowdate  = new DateTime();
$interval = $nowdate-&gt;diff($date);
var_dump($interval);
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=2

;Тема
Topic="Календарные классы php"
TopicId=206


;Автор
Author="Федотов М.В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "single"

Single = "Массив содержащий информацию о разнице времен \$date и \$nowdate(количество лет, месяцев, дней и т.д.)"
ResponseOptions[]="Массив содержащий информацию о разнице времен \$date и \$nowdate(количество лет, месяцев, дней и т.д.)"
ResponseOptions[]="Число секунд между \$date и \$nowdate"
ResponseOptions[]="Строка содержащая разницу между датами \$date и \$nowdate"

