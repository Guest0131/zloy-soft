[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=3

;Условие
Text="Необходимо задать синий цвет фона у поля ввода пароля. Какой стиль подойдет для этой цели?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=4

;Тема
Topic="Селектор атрибута"

;ID Темы
TopicId=179

;Автор
Author="Сорокин С.Н."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "input[type=\"password\"] {background: #00f;}"
ResponseOptions[]="input[type=\"password\"] {background: #00f;}"
Multi[] = "input[type*=\"password\"] {background: #0000ff;}"
ResponseOptions[]="input[type*=\"password\"] {background: #0000ff;}"
Multi[] = "input[type=\"passsword\"] {background: blue;}"
ResponseOptions[]="input[type=\"passsword\"] {background: blue;}"
Multi[] = "input[type^=\"date\"] {background: #f00;}"
ResponseOptions[]="input[type^=\"date\"] {background: #f00;}"
ResponseOptions[]="input[type=\"inputpassword\"] {background: #00f;}"
ResponseOptions[]="input[type=\"date-type\"] {background: #f00;}"

