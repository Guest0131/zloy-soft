[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Как можно организовать перебор всех элементов массива mass?</p><p><pre class=\"prettyprint linenums no-scroll\"><code>
&lt;?php
    $mass = array(\"php\", \"c\", \"c++\");    
</code></pre></p><p>"

;Сложность в диапазоне [1-5]
Difficulty=1

;Тема
Topic="Перебор элементов массива"
TopicId=215


;Автор
Author="Тележников В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "foreach(\$mass as \$magic)"
ResponseOptions[]="foreach(\$mass as \$magic)"
Multi[] = "for(\$i=0; \$i < count(\$mass); \$i++)"
ResponseOptions[]="for(\$i=0; \$i < count(\$mass); \$i++)"
Multi[] = "foreach(\$mass as &\$magic)"
ResponseOptions[]="foreach(\$mass as &\$magic)"
ResponseOptions[]="foreach(\$mass as *\$magic)"
ResponseOptions[]="foreach(\$mass)"

