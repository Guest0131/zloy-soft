[Info]

;Вариант. 0 - если у задачи нет вариантов. Числа 1, 2, ... если есть.
Variant=0

;Условие
Text="Какая конструкция позволяет  в теле цикла удалить элементы исходного массива (списка) только с использованием функции unset()?</p><p>"

;Сложность в диапазоне [1-5]
Difficulty=3

;Тема
Topic="Перебор элементов массива"
TopicId=215


;Автор
Author="Тележников В."

;Имя решателя (не изменять)
Solver="Test"

[Solution]
Type = "multi"

Multi[] = "foreach(\$array_arr as \$index=>\$value)"
ResponseOptions[]="foreach(\$array_arr as \$index=>\$value)"
Multi[] = "for(\$i=0; \$i < count(\$array_arr); \$i++)"
ResponseOptions[]="for(\$i=0; \$i < count(\$array_arr); \$i++)"
ResponseOptions[]="foreach(\$array_arr as &\$element)"
ResponseOptions[]="foreach(\$array_arr as \$element)"

