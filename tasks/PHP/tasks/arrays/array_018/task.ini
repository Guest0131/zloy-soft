[Info]

Variant=0

Text="<p>Реализовать функцию css_optimize($__code), на вход которой поступает массив $__code вида ['селектор' => ['свойство1', 'свойство2'], ...].
Функция возвращает массив такого же вида, но с объединенными селекторами для одинаковых свойств, например: </p>
<p>['a' => ['color: red', 'font-weight: 400'], 'h1' => ['color: red', 'font-weight: 500'] необходимо преобразовать в: ['a, h1' => ['color: red'], 'a' => ['font-weight: 400'], 'h1' => ['font-weight: 500']</p>
<p>Порядок названий в объединенном селекторе выстраивается в соответствие с первоначальным видом, например, если в исходном тексте был порядок ['a' => ..., 'h2' => ..., 'h1' => ...] то объединенный селектор выглядит так: 'a, h2, h1' </p>
<p>Для определенности, полученный массив отсортировать при помощи ksort() и подмассивы свойств при помощи sort().</p>"

Difficulty=5

Topic="Задачи на массивы"
TopicId=253

Author="AoF"

Solver="PHP"



[DataBase]
TaskId=2410
GroupTaskId=1557
