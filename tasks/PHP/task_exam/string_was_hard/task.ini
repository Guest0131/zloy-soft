[Info]

Variant=0

Text="<p>Написать функцию matrix_multiply($__left_matrix, $__right_matrix), которая осуществляет умножение матриц.</p>
<p>Каждая матрица представляет собой строку вида 
<pre>a b c d
e f g h 
...
w x y z,</pre> в которой строки матрицы разделены '\n', а столбцы - пробелами.</p>
<p>Функция возвращает строку, содержащую результат умножения матриц в описаном формате.</p>
<p>Функция возвращает false, если матрицы неполные или размерность не позволяет произвести умножение.</p>"

Difficulty=4

Topic="Задачи на экзамен"
TopicId=257

Author="dCm"

Solver="PHP"

